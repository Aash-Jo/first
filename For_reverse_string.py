# e.g. user gave AADI , print IDAA
#whatever!!!NEXT.
# ask user for a string
vStr=input("enter a string:")
# print the reverse of the string
vCount=0
for n in (vStr):#[a,a,d,i]
  vCount=vCount+1


while vCount!=0:
  vIndex=vCount-1
  print(vStr[vIndex])
  vCount=vCount-1